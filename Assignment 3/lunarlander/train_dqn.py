"""DQN on LunarLander-v3 (discrete). Q2.2.4(c).

Placeholder. The PA2 submission already contains a working DQN implementation
(see ../../Assignment 2/train.py). Options:
    - Import / adapt that script to write a `progress.csv` in the same schema
      as the SAC runs so plot.py can compare curves apples-to-apples.
    - Or keep DQN training fully in Assignment 2 and just copy the CSV into
      `runs/dqn_seed{n}/progress.csv` here for plotting.

Expected CSV schema (for plot.py compatibility):
    step, return_mean, return_std, alpha (blank), wall_time
"""

if __name__ == '__main__':
    raise NotImplementedError(
        "Plug in the PA2 DQN implementation here — see Assignment 2/train.py.\n"
        "Output `progress.csv` with columns "
        "(step, return_mean, return_std, alpha, wall_time).")
